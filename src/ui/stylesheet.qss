/* ==========================================================================
   SYNCADEMIC APPLICATION STYLE (QSS)
   ========================================================================== */

/* ──────────────────────────────────────────────────────────────────────────
   GROUP 1: THE CORE APP BACKGROUND
   This section sets the main dark background and default font for the entire app.
   ────────────────────────────────────────────────────────────────────────── */
QMainWindow, QDialog, QWidget, QTabWidget::pane {
    background-color: @COLOR_BACKGROUND@;
    color: @COLOR_PRIMARY@;
    font-family: 'Segoe UI', Arial, sans-serif;
    font-size: 12px;
}

/* ──────────────────────────────────────────────────────────────────────────
   GROUP 2: DEEP SIDEBAR & VIEWPORTS
   These components use the deepest dark tone (the sidebar and lists background).
   ────────────────────────────────────────────────────────────────────────── */
QWidget#sidebar, QListWidget, QTableWidget, QScrollBar, QTabBar::tab {
    background-color: @COLOR_SIDEBAR@;
}

/* ──────────────────────────────────────────────────────────────────────────
   GROUP 3: CARDS, CONTAINERS & PAPERS
   These elements sit on top of the background like cards (inputs, panels, alerts).
   ────────────────────────────────────────────────────────────────────────── */
QGroupBox, QPushButton, QTableWidget::alternate-background-color, 
QHeaderView, QHeaderView::section, QDateEdit, QCalendarWidget, QMessageBox {
    background-color: @COLOR_CARD_BG@;
}

/* ──────────────────────────────────────────────────────────────────────────
   GROUP 4: INTERACTION STATES (HOVER & SELECTION)
   What happens when a user hovers or clicks on buttons, rows, and items.
   ────────────────────────────────────────────────────────────────────────── */
QPushButton:hover, QPushButton#generateBtn, QListWidget::item:hover, 
QTableWidget::item:selected, QHeaderView::section:hover, QTabBar::tab:hover:!selected {
    background-color: @COLOR_HOVER@;
}

QListWidget::item:selected, QTableWidget::item:selected, QTabBar::tab:selected {
    color: @COLOR_SECONDARY@; /* Highlights selected items with the Lavender color */
}

/* ──────────────────────────────────────────────────────────────────────────
   GROUP 5: SPECIAL COMPONENT TWEAKS
   A few tiny functional rules to make specific buttons or borders look polished.
   ────────────────────────────────────────────────────────────────────────── */

/* Gives the main left sidebar a nice separation line on the right */
QWidget#sidebar { border-right: 1px solid rgba(255, 255, 255, 0.1); }

/* The big high-priority "Generate" action button */
QPushButton#generateBtn {
    border: 1px solid @COLOR_PRIMARY@;
    font-size: 13px;
    font-weight: bold;
}

/* Containers and scrolls that should remain completely invisible/transparent */
QLabel, QScrollArea, QScrollArea > QWidget > QWidget { background: transparent; }

/* Custom design borders for container boxes and date inputs */
QGroupBox, QListWidget, QDateEdit { border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 4px; }